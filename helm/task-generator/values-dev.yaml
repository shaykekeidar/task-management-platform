namespace: task-generator-dev

frontend:
  replicas: 1
  image:
    repository: shaykekeidar/kub-demo-frontend
    tag: "18" # need copy manually from dev until script will do it.
  config:
    FRONTEND_VERSION: "18"
  ingress:
    host: task-generator-dev.local
    tlsSecretName: frontend-tls
  service:
    port: 80
    targetPort: 80
  resources:
    requests:
      cpu: "100m"
      memory: "128Mi"
    limits:
      cpu: "500m"
      memory: "512Mi"
  hpa:
    minReplicas: 1
    maxReplicas: 5
    cpuTarget: 50

users:
  replicas: 1
  image:
    repository: shaykekeidar/kub-demo-users
    tag: "8"
  config:
    USERS_VERSION: "8"
  service:
    port: 8080
    targetPort: 8080

auth:
  replicas: 1
  image:
    repository: shaykekeidar/kub-demo-auth
    tag: "6"
  config:
    AUTH_VERSION: "6"
  service:
    port: 80
    targetPort: 80

tasks:
  replicas: 1
  image:
    repository: shaykekeidar/kub-demo-tasks
    tag: "13"
  config:
    TASKS_VERSION: "13"
  service:
    port: 8000
    targetPort: 8000
  persistence:
    pvName: host-pv-dev
    claimName: host-pvc-dev
    storageClassName: standard
    hostPath: /data/task-generator-dev/tasks
    storage: 20Mi
postgres:
  image:
    repository: postgres
    tag: "17"

  database: tasksdb
  user: taskuser
  password: taskpass

  service:
    port: 5432

  storage:
    size: 1Gi

migration:
  enabled: false
  jobName: tasks-file-to-postgres-migration-v1
  oldPvcClaimName: host-pvc-dev
  oldDataMountPath: /app/tasks
  oldFileName: tasks.db